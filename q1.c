#include<stdio.h>
int main()
{
 	int a,b,sum,diff,product;
	printf(" enter two numbers %d%d ", a,b);
	scanf("%d%d",&a,&b);
	sum = a+b;
	diff= a-b;
	product=a*b;
	printf(" sum=%d\n diff=%d\n product=%d",sum,diff,product);

	return 0;
}
